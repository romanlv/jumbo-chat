[build]
builder = "RAILPACK"

[deploy]
preDeployCommand = "bun scripts/deploy-migrate.ts"
startCommand = "bun src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
